const {db}=require("../lib/db"),{auth}=require("../lib/auth");module.exports=async(req,res)=>{try{let p=auth(req,["admin"]),s=db();if(req.method!=="POST")return res.status(405).json({error:"Method"});for(let [k,v] of Object.entries(req.body||{}))await s`INSERT INTO settings(key,value) VALUES(${k},${String(v)}) ON CONFLICT(key) DO UPDATE SET value=EXCLUDED.value,updated_at=now()`;await s`INSERT INTO audit(actor_user_id,action,detail) VALUES(${p.uid},'SETTINGS_UPDATE','Site settings updated')`;res.json({ok:true})}catch(e){res.status(e.status||500).json({error:e.message})}}