const {db}=require("../lib/db"),bcrypt=require("bcryptjs"),{tokenFor}=require("../lib/auth");module.exports=async(req,res)=>{try{let {email,password}=req.body||{},s=db(),r=await s`SELECT * FROM users WHERE email=${(email||"").toLowerCase().trim()}`;if(!r.length||!await bcrypt.compare(password||"",r[0].password_hash))return res.status(401).json({error:"Email və ya şifrə yanlışdır"});let u=r[0];res.json({user:{id:u.id,account_id:u.account_id,name:u.name,email:u.email,role:u.role,balance:u.balance},token:tokenFor(u)})}catch(e){res.status(500).json({error:e.message})}}